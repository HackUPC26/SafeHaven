# Auto-discovery via Expo Go is the default — bridge.js reads the laptop IP
# from Constants.expoConfig.hostUri.
# Uncomment to override (e.g. for production builds):
# EXPO_PUBLIC_RELAY_URL=ws://192.168.1.10:8080
